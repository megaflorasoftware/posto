{
  "name": "posto",
  "version": "0.7.1",
  "description": "",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "pnpm --filter desktop dev",
    "dev": "pnpm --filter desktop dev",
    "build": "pnpm --filter desktop build",
    "ios:device": "tauri ios build --export-method debugging && node scripts/install-ios-device.mjs",
    "serve": "pnpm --filter desktop serve",
    "tauri": "tauri"
  },
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
